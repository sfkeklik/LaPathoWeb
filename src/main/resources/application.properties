# Default profile - PostgreSQL for both development and deployment
spring.application.name=LaPatho
tile.block-size=4096
tile.output-base-path=C:/Users/user/Desktop/openslide/tiles
tile.size=512

# Upload configuration
upload.base-path=uploads

# PostgreSQL Database configuration (development)
spring.datasource.url=jdbc:postgresql://localhost:5432/lapatho
spring.datasource.username=lapatho
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# Schema initialization - Only run on first setup
spring.sql.init.mode=never
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.continue-on-error=false

# JPA/Hibernate configuration - Tables are preserved
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.type=info
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# File upload configuration
spring.servlet.multipart.max-file-size=2048MB
spring.servlet.multipart.max-request-size=2048MB

# Server configuration
server.port=8080
